@page
@model EsperiaHelp.Pages.Classrooms.IndexModel

@{
    ViewData["Title"] = "Aule";
}

<h1 class="text-danger">Lista delle aule</h1>

<div class="table-responsive mt-5">
    <table class="table table-hover text-center">
        <thead class="bg-danger text-white">
            <tr>
                <th>
                    Aula
                </th>
                <th></th>
            </tr>
        </thead>
        <!--<tbody>
    @foreach (var item in Model.Classroom) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
    }
        </tbody>-->
    <tbody>
        <tr>
            <td>
                I1-6
            </td>
            <td>
                <a asp-page="./Edit" class="btn btn-warning"><i class="fa fa-pencil-square-o" style="color: white;"></i></a> |
                <a asp-page="./Delete" class="btn btn-danger"><i class="fa fa-remove" style="color: white;"></i></a>
            </td>
        </tr>
        <tr>
            <td>
                1-18
            </td>
            <td>
                <a asp-page="./Edit" class="btn btn-warning"><i class="fa fa-pencil-square-o" style="color: white;"></i></a> |
                <a asp-page="./Delete" class="btn btn-danger"><i class="fa fa-remove" style="color: white;"></i></a>
            </td>
        </tr>
    </tbody>
    </table>
</div>
